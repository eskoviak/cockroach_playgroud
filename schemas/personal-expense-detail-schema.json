{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://eskoviak.com/schemas/personal-expense-detail-schema.json",
    "title": "Personal Expense Detail",
    "description": "Detail object added to a Personal expense entry",
    "type": "object",
    "properties": {
        "location" : {
            "description" : "The location of the expense",
            "type" : "string"
        },
        "vendor" : {
            "description" : "the name of the vendor providing the service",
            "type" : "string"
        }
    },
    "required": ["vendor", "location"]
}